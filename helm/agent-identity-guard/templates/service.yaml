apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-identity-guard.fullname" . }}
  labels:
    {{- include "agent-identity-guard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "agent-identity-guard.selectorLabels" . | nindent 4 }}
